- name: Install PostgreSQL
  ansible.builtin.apt:
    name:
      - postgresql
      - postgresql-client
    state: present

- name: Enable PostgreSQL
  ansible.builtin.service:
    name: postgresql
    enabled: true
    state: started
